#include <iostream>
using namespace std;
int main() {
   int arr[];
   cout<<"Enter the elements";
   for(i=0;i<5;i++)
   {
   cin>>a[];
   cout<<a;
   }
   int *ptr = &arr[0];
   cout<<"The values in the array are: ";
   for(int i = 0; i < 5; i++) {
      cout<< *ptr <<" ";
      ptr++;
   }
   return 0;
}
